while True:
  ready = None
  while ready != "y":
    ready = input("Are you ready to play? (y/n) ").lower()
  
  
  if ready == "y":
    game_on = True
    
    if game_on:
      deal(deck, player1)
      deal(deck, computer)
    
      computer.list[0].print_card()
      print()
      print()
      player1.list[0].print_card(), player1.list[1].print_card()
