def deal(deck, hand):
  hand.list[0] = deck[random.randint(0,14)]
  hand.list[1] = deck(random.randint(0,14)]

def hit(hand):
  hand.append(deck[random.randint(0,14)])
  
def bust_check(hand):
  #return True if busted 
  return sum(hand) > 21:
  
def hit_stay():
  decision = None
  while decision != "h" or decision != "s":
    decision = raw_input("Choose hit or stay (h/s): ")
    
  
  
